import React from 'react';
import style from './Authorization.module.css';

const Authorization = () => {
  return (
    <div className={style.authorizationContainer}>
      <p >Authorization in the development process</p>
    </div>
  );
}

export default Authorization
