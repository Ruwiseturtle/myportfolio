import { PER_PAGE } from "../../Services/GlobalVariables";
import projects from "../../data/Myprojects.json";

export const requestGetProjects = async (page, itemFilter) => {
//   const filteredProjects = projects.filter(
//     (project) => project.frameworks === itemFilter
//     );  
    
//   const start = (page - 1) * PER_PAGE;
//   return filteredProjects.slice(start, start + PER_PAGE);
};
